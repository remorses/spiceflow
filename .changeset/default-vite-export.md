---
'spiceflow': patch
---

Add default export to `spiceflow/vite` so you can write `import spiceflow from 'spiceflow/vite'` instead of the named import. The previous named export `spiceflowPlugin` is still available but deprecated.
