---
'spiceflow': minor
---

Add `app.getRoutes()` to list public route metadata from the app and mounted sub-apps, making it easy to discover page paths for files such as `sitemap.xml` while keeping handlers and internal route state private.
