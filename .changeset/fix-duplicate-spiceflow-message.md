---
'spiceflow': patch
---

Improve the duplicate `spiceflow` runtime error message to point users toward deduping their install instead of changing `optimizeDeps.exclude`, which Spiceflow already configures automatically.
