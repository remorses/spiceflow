---
'spiceflow': patch
---

Clarify in the README that only the root `/*` layout should render the HTML document shell, and document that scoped wildcard layouts like `/app/*` and `/docs/*` also wrap their base paths such as `/app` and `/docs`.
