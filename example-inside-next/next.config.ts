import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // The catch-all route imports from example-basepath/dist which is
  // already compiled JS, so no transpilation config is needed.
}

export default nextConfig
