{
  "name": "example-sdk",
  "version": "0.3.0",
  "description": "SDK for https://x",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "jsonrpc-sdk --url http://localhost:3333",
    "serve": "jsonrpc-sdk serve --watch --port 3333",
    "watch": "jsonrpc-sdk --watch --url http://localhost:3333"
  },
  "dependencies": {
    "jsonrpc-sdk": "workspace:*"
  },
  "devDependencies": {
    "example-app": "workspace:*"
  }
}
